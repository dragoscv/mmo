{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "main-capability",
    "description": "Default capabilities for the main window of the MMO native shell.",
    "windows": ["main"],
    "permissions": ["core:default", "shell:allow-open"]
}
