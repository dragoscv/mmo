// Thin re-export: primitives live in packages/ui (@mmo/ui, Base UI). Keep this path for call sites.
export { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter, CardAction } from "@mmo/ui";
