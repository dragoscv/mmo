// Thin re-export: primitives live in packages/ui (@mmo/ui, Base UI). Keep this path for call sites.
export { Table, TableHeader, TableBody, TableFooter, TableRow, TableHead, TableCell, TableCaption } from "@mmo/ui";
