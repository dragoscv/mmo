{
  "name": "@mmo/sdk",
  "version": "0.0.1",
  "private": true,
  "description": "Thin TypeScript client for MMO's REST + SSE API. Drop-in replacement for in-process @mmo/ai when calling MMO from external projects. ESM-only, zero React deps.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
