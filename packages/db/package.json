{
  "name": "@mmo/db",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Drizzle schema for MuzicAI — single source of truth consumed by apps/web (Next.js) and apps/gateway (Hono).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./schema-ai": "./src/schema-ai.ts",
    "./schema-projects": "./src/schema-projects.ts",
    "./schema-projects-normalized": "./src/schema-projects-normalized.ts",
    "./schema-training": "./src/schema-training.ts"
  },
  "peerDependencies": {
    "drizzle-orm": "^0.45.2"
  }
}
