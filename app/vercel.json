{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "framework": "nextjs",
    "buildCommand": "pnpm build",
    "installCommand": "pnpm install --frozen-lockfile --ignore-workspace",
    "outputDirectory": ".next",
    "crons": [
        {
            "path": "/api/cron/training-reconcile",
            "schedule": "*/2 * * * *"
        }
    ]
}